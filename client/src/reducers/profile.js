const initialState = {
    profile: null,
    profiles: [],
    repos: [],
    loading: null,
    error: null
}


export default (state = initialState, action)=>{
    switch(action.type){
        default:
            return state
    }
}